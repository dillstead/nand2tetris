Rect

Breakout
init()
  - draws bottom of the playfield
  - draws score
  - positions and draws the paddle
  - positions, sets direction, and draws the ball

play()
  - game loop

read keyboard input
move ball
move paddle

direction is left or right

Paddle
init(rect)
move(direction) - return cur rect
getPrev() - return prev rect
getCur() - return cur rect
drawPrev() - draws prev location
drawCur() - draws cur location 
clearPrev() - clears prev location
clearCur() - clears cur location 

Ball
init(rect, d0, d1)
setDir(d0, d1)
move() - return cur rect
getPrev() - return prev rect
getCur() - return cur rect
clearPrev() - clears prev location
clearCur() - clears cur location 
bounce() - ?







